export default function Loading() {
  return (
    <main className="flow-shell" aria-busy="true" aria-live="polite">
      <section className="flow-card">
        <p className="eyebrow">SimpleWay Drawing</p>
        <h1 className="flow-title">Carregando sua próxima etapa…</h1>
        <p className="lead compact">Estamos preparando apenas o contexto necessário para continuar sua jornada.</p>
      </section>
    </main>
  );
}
